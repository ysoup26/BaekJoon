#include <iostream>

using namespace std;

int main()
{
    int t_k=1,t_q=1,t_r=2,t_b=2,t_kn=2,t_p=8;
    int k,q,r,b,kn,p;
    cin>>k>>q>>r>>b>>kn>>p;
    cout<<t_k-k<<' '<<t_q-q<<' '<<t_r-r<<' '<<t_b-b<<' '<<t_kn-kn<<' '<<t_p-p;
    return 0;
}